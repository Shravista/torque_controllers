torque_controller:
  joints: {
    type: string_array,
    default_value: [],
    description: "Name of the joints to control",
  }
  interface_name: {
    type: string,
    default_value: "",
    description: "Name of the interface to command",
  }
  K: {
    type: double_array,
    default_value: [],
    description: "The gains for holding the robot position in a given place",
  }
  urdf_relative_path: {
    type: string,
    default_value: "",
    description: "This urdf path should given relative the package e.g. <package_name>/urdf/robot.urdf",
  }